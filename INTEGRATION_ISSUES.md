# 🔍 Проблемы интеграции между компонентами

## ❌ **НАЙДЕНЫ НЕСОВМЕСТИМОСТИ:**

### 1. **Порты и URL-ы**
- **Backend**: `http://localhost:8003/api/v1/`
- **Админка**: `/api/v1` (относительный - ожидает тот же порт)  
- **Mobile**: `AppConfig.apiBaseUrl` (неизвестно какой)

### 2. **Аутентификация endpoints**
- **Backend имеет**: `/auth/otp/verify/`, `/auth/otp/request/`
- **Mobile ожидает**: `/auth/token/refresh/` (не существует!)
- **Админка**: пытается использовать `/auth/login/` (тоже нет!)

### 3. **JWT токены** 
- Backend генерирует MongoDB-совместимые JWT
- Но endpoints для refresh токенов не совпадают

## ✅ **ЧТО НУЖНО ИСПРАВИТЬ:**

### Админка (React):
```typescript
// Изменить baseURL на абсолютный
baseURL: 'http://localhost:8003/api/v1'

// Изменить auth endpoint 
'/auth/otp/verify/' вместо '/auth/login/'
```

### Mobile (Flutter):
```dart
// Обновить AppConfig.apiBaseUrl
'http://localhost:8003/api/v1'

// Изменить refresh endpoint
'/auth/otp/verify/' вместо '/auth/token/refresh/'
```

### Backend:
```python  
# Добавить endpoint для refresh токенов
path('auth/token/refresh/', refresh_token_view)
```

## 🚨 **ТЕКУЩИЙ СТАТУС: НЕ СОВМЕСТИМЫ**

Компоненты не смогут взаимодействовать без исправлений!


