import 'package:flutter/material.dart';

class SearchPartnersScreen extends StatelessWidget {
  const SearchPartnersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Search Partners'),
      ),
      body: const Center(
        child: Text('Search Partners Screen'),
      ),
    );
  }
}

